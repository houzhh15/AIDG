# AIDG 部署指南 - 让搭建变得简单 🚀

> 欢迎来到 AIDG 的部署世界！这篇指南会像老朋友聊天一样，带你一步步完成整个系统的搭建。
> 别担心，就算你不是技术专家，跟着步骤走也能顺利部署完成！

---

## 📚 目录

1. [这是什么？](#这是什么)
2. [我能用它做什么？](#我能用它做什么)
3. [准备工作](#准备工作)
4. [部署方案选择](#部署方案选择)
5. [方案一：基础版（推荐新手）](#方案一基础版推荐新手)
6. [方案二：完整版（需要会议功能）](#方案二完整版需要会议功能)
7. [常见问题](#常见问题)
8. [获取帮助](#获取帮助)

---

## 这是什么？

**AIDG (AI-Dev-Gov)** 是一个帮助你管理项目、记录会议、协同开发的智能化工具。

想象一下：
- 📝 **项目管理**：就像有个电子助手帮你整理所有项目资料
- 🎙️ **会议记录**：录音自动转文字，还能识别谁说了什么
- 🤖 **AI 辅助**：像有个编程小伙伴随时帮你查资料、写代码
- 📊 **进度追踪**：一目了然地看到项目进展

---

## 我能用它做什么？

### 基础功能（所有人都能用）

这些功能只需要一个轻量级的镜像（不到 100MB），就像安装一个小应用一样简单：

| 功能模块 | 你能干什么 | 对应特性 |
|---------|----------|---------|
| 📋 **项目管理** | 创建项目、管理任务、写需求文档 | MOD-02, MOD-03 |
| 🤖 **AI 辅助** | 连接 Claude/ChatGPT，让 AI 帮你写代码 | MOD-04, MOD-05 |
| 📁 **文档管理** | 组织各种技术文档，建立知识库 | MOD-06 |
| 🎯 **执行计划** | 让 AI 帮你规划任务，一步步执行 | MOD-07 |

### 高级功能（需要额外组件）

如果你需要会议录音转写功能，需要额外安装一些组件（会占用更多空间）：

| 功能模块 | 你能干什么 | 需要的组件 | 对应特性 |
|---------|----------|----------|---------|
| 🎙️ **会议录音** | 把录音自动变成文字 | Whisper | MOD-01 |
| 👥 **说话人识别** | 识别谁在说话 | PyAnnote | MOD-01 |
| 🎬 **音频处理** | 剪辑、转换音频格式 | FFmpeg | MOD-01 |

---

## 准备工作

### 1. 检查你的电脑

确保你的电脑有：
- ✅ **Docker Desktop**（必须）- [下载地址](https://www.docker.com/products/docker-desktop)
- ✅ **至少 4GB 空闲内存**（基础版）或 **8GB**（完整版）
- ✅ **至少 5GB 磁盘空间**（基础版）或 **15GB**（完整版）

### 2. 安装 Docker Desktop

如果你还没安装 Docker：

**Windows 用户**：
1. 下载并安装 [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
2. 安装完成后重启电脑
3. 打开 Docker Desktop，确保它在运行

**Mac 用户**：
1. 下载并安装 [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop)
2. 打开 Docker Desktop，确保它在运行

**如何确认 Docker 安装成功？**

打开终端（Windows 用户打开 PowerShell），输入：
```bash
docker --version
```

如果看到类似 `Docker version 24.0.5` 的输出，说明安装成功！

### 3. 下载 AIDG 项目

有两种方式：

**方式 A：使用 Git（推荐）**
```bash
git clone https://github.com/houzhh15/AIDG.git
cd AIDG
```

**方式 B：直接下载 ZIP**
1. 访问 https://github.com/houzhh15/AIDG
2. 点击绿色的 "Code" 按钮
3. 选择 "Download ZIP"
4. 解压到你喜欢的位置

---

## 部署方案选择

### 🤔 我应该选哪个？

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  你只是想用项目管理和 AI 辅助功能？                      │
│                                                         │
│  是 ──▶  选择【方案一：基础版】                          │
│                                                         │
│  不，我还需要会议录音转文字功能 ──▶  选择【方案二：完整版】│
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 方案对比

| 对比项 | 基础版 🌟 | 完整版 🚀 |
|-------|---------|----------|
| **镜像大小** | ~100MB（超轻量） | ~2.5GB（包含AI模型） |
| **启动时间** | 10秒 | 30-60秒 |
| **内存占用** | 2GB | 4-6GB |
| **磁盘空间** | 5GB | 15GB |
| **会议录音** | ❌ 不支持 | ✅ 支持 |
| **项目管理** | ✅ 完整支持 | ✅ 完整支持 |
| **AI 辅助** | ✅ 完整支持 | ✅ 完整支持 |
| **适合人群** | 个人开发者、小团队 | 需要会议记录的团队 |

---

## 方案一：基础版（推荐新手）

### 🎯 适合你的情况

- ✅ 只需要项目管理和 AI 辅助功能
- ✅ 电脑配置一般（4GB 内存也能跑）
- ✅ 想快速开始使用
- ❌ 暂时不需要会议录音转文字

### 📦 需要的镜像

只需要**1个镜像**：
- `aidg-aidg`（主程序，不到 100MB）

### 🚀 开始部署

#### 步骤 1：创建数据目录

在终端中运行：
```bash
# 进入 AIDG 项目目录
cd AIDG

# 创建数据存储目录
mkdir -p data/projects data/users data/meetings data/audit_logs
```

**这是干什么的？**
> 这些文件夹用来存储你的项目数据、用户信息等。就像给系统准备好储物柜。

#### 步骤 2：启动服务

```bash
# 使用 Docker Compose 启动
docker-compose up -d
```

**会发生什么？**
```
[+] Running 2/2
 ✔ Network aidg-network  Created
 ✔ Container aidg-unified  Started
```

看到这个输出就说明启动成功了！

#### 步骤 3：验证服务

打开浏览器，访问这些地址：

- 🌐 **Web 界面**: http://localhost:8000
- 🤖 **MCP 服务**: http://localhost:8081/health

如果能打开页面，恭喜你，部署成功！ 🎉

#### 步骤 4：首次登录

1. 打开 http://localhost:8000
2. 默认账号：`admin`
3. 默认密码：`admin123`
4. **重要**：登录后立即修改密码！

### 📝 配置说明

基础版的所有配置都在 `docker-compose.yml` 文件中，主要环境变量：

```yaml
environment:
  # 基础配置
  - ENV=development           # 运行环境（开发模式）
  - PORT=8000                 # Web 服务端口
  - MCP_HTTP_PORT=8081        # MCP 服务端口
  
  # 安全配置（生产环境请修改！）
  - JWT_SECRET=dev-secret-change-me-in-production
  - ADMIN_DEFAULT_PASSWORD=admin123
  - MCP_PASSWORD=dev-mcp-password
  
  # 数据目录
  - PROJECTS_DIR=/app/data/projects
  - USERS_DIR=/app/data/users
  - MEETINGS_DIR=/app/data/meetings
```

**需要修改的地方（生产环境）：**
- `JWT_SECRET`: 改成一个复杂的随机字符串（至少32位）
- `ADMIN_DEFAULT_PASSWORD`: 改成强密码
- `MCP_PASSWORD`: 改成强密码

---

## 方案二：完整版（需要会议功能）

### 🎯 适合你的情况

- ✅ 需要会议录音转文字功能
- ✅ 需要识别不同说话人
- ✅ 电脑配置较好（8GB+ 内存）
- ✅ 有足够的磁盘空间（15GB+）

### 📦 需要的镜像

需要**3个镜像**：
1. `aidg-aidg:lite-test`（主程序，~100MB）
   - 容器名称：`aidg-unified`
   - 端口：8000（Web）、8081（MCP）
2. `aidg-deps-service:latest`（依赖服务，~2GB，包含 FFmpeg 和 PyAnnote）
   - 容器名称：`aidg-deps-service`
   - 端口：8080
3. `ghcr.io/mutablelogic/go-whisper:latest`（Whisper 服务，~500MB）
   - 容器名称：`aidg-whisper`
   - 端口：8082（映射到容器内部的80端口）

**容器间通信：**
- 所有容器通过 `aidg-network` 桥接网络连接
- 主服务通过容器名称访问其他服务（如 `http://aidg-whisper:80`）

### 🚀 开始部署

#### 步骤 1：准备工作

##### 1.1 创建数据目录

```bash
cd AIDG

# 创建所有需要的目录
mkdir -p data/{projects,users,meetings,audit_logs}
mkdir -p models/{whisper,huggingface}
mkdir -p config
```

##### 1.2 获取 HuggingFace Token（重要！）

**为什么需要这个？**
> PyAnnote（说话人识别）需要从 HuggingFace 下载 AI 模型，需要一个免费的访问令牌。

**如何获取：**
1. 访问 https://huggingface.co/settings/tokens
2. 登录或注册账号（免费）
3. 点击 "New token"
4. 输入名称（如 "AIDG"），选择 "Read" 权限
5. 复制生成的 token（类似 `hf_xxxxxxxxxxxxx`）

**保存 Token：**
```bash
# 在终端中设置环境变量
export HUGGINGFACE_TOKEN=hf_你的token
```

**Windows 用户（PowerShell）：**
```powershell
$env:HUGGINGFACE_TOKEN="hf_你的token"
```

#### 步骤 2：构建 deps-service 镜像

**这是干什么的？**
> deps-service 是一个独立的服务，包含了 FFmpeg（音频处理）和 PyAnnote（说话人识别）。

```bash
# 运行构建脚本
./scripts/build-deps-service.sh
```

**预期输出：**
```
========================================
  AIDG Deps-Service Build
========================================

📋 Configuration:
  Dockerfile: Dockerfile.deps
  Image Name: aidg-deps-service
  Image Tag: latest

🔨 Building Docker image...
...
✅ Build Successful!
```

**如果构建失败？**
- 检查 Docker Desktop 是否在运行
- 确保有足够的磁盘空间（至少 10GB）
- 查看完整错误信息，参考[常见问题](#常见问题)

#### 步骤 3：启动所有服务

```bash
# 使用完整配置启动
docker-compose -f docker-compose.deps.yml up -d
```

**会发生什么？**
```
[+] Running 4/4
 ✔ Network aidg-network           Created
 ✔ Container aidg-whisper          Started
 ✔ Container aidg-deps-service     Started
 ✔ Container aidg-unified          Started
```

#### 步骤 4：验证所有服务

```bash
# 检查服务状态
docker-compose -f docker-compose.deps.yml ps
```

**预期输出：**
```
NAME                IMAGE                              STATUS
aidg-unified        aidg-aidg:lite-test                Up (healthy)
aidg-deps-service   aidg-deps-service:latest           Up (healthy)
aidg-whisper        ghcr.io/.../go-whisper:latest      Up
```

**健康检查说明：**
- `aidg-unified`: 通过 `wget http://localhost:8000/health` 检查（30秒间隔）
- `aidg-deps-service`: 通过 `curl http://localhost:8080/api/v1/health` 检查（10秒间隔）
- `aidg-whisper`: 无健康检查配置，只要启动即认为正常

**访问各个服务：**
- 🌐 **Web 界面**: http://localhost:8000
- 🤖 **MCP 服务**: http://localhost:8081/health
- 🎙️ **Whisper 服务**: http://localhost:8082/ （端口映射 8082:80）
- 🔧 **Deps 服务**: http://localhost:8080/api/v1/health （端口 8080:8080）
- 📊 **服务状态检查**: http://localhost:8000/api/v1/services/status （无需认证）

如果所有服务都能访问，恭喜你，完整版部署成功！ 🎊

#### 步骤 5：验证服务集成

完整版部署后，系统会自动检查各个依赖服务的可用性。你可以通过以下方式验证：

**方法1：访问服务状态API（推荐）**
```bash
curl http://localhost:8000/api/v1/services/status
```

**预期返回：**
```json
{
  "whisper_available": true,
  "deps_service_available": true,
  "whisper_mode": "available",
  "dependency_mode": "available"
}
```

**方法2：在Web界面中检查**
- 登录后，打开会议管理页面
- 如果能看到 "Chunk列表" 和 "Chunk详情" 功能，说明服务正常
- 如果这些功能被隐藏，说明依赖服务不可用

**如果服务不可用：**
1. 检查容器是否都在运行：`docker ps --filter "name=aidg"`
2. 检查容器日志：
   - `docker logs aidg-whisper`
   - `docker logs aidg-deps-service`
3. 确认网络连接：容器间应该能互相访问

### 📝 完整版配置说明

在 `docker-compose.deps.yml` 中有更多配置选项：

```yaml
services:
  # Whisper 转录服务
  whisper:
    image: ghcr.io/mutablelogic/go-whisper:latest
    container_name: aidg-whisper
    ports:
      - "8082:80"
    volumes:
      - ./models/whisper:/data         # 模型存储
      - ./data/meetings:/output        # 输出目录
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G

  # Deps-Service（FFmpeg + PyAnnote）
  deps-service:
    image: aidg-deps-service:latest
    container_name: aidg-deps-service
    ports:
      - "8080:8080"
    environment:
      - LOG_LEVEL=info
      - HUGGINGFACE_TOKEN=${HUGGINGFACE_TOKEN}  # 从环境变量读取
      - HF_HOME=/models/huggingface
      - TRANSFORMERS_CACHE=/models/huggingface
      - TORCH_HOME=/models/huggingface
    volumes:
      - ./data:/data
      - ./config:/app/config:ro
      - ./models/huggingface:/models/huggingface
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G
        reservations:
          cpus: '2'
          memory: 4G

  # 主服务
  aidg:
    image: aidg-aidg:lite-test
    container_name: aidg-unified
    depends_on:
      whisper:
        condition: service_started
      deps-service:
        condition: service_healthy
    environment:
      # 依赖服务配置
      - DEPENDENCY_MODE=remote
      - DEPS_SERVICE_URL=http://aidg-deps-service:8080
      - DEPENDENCY_SHARED_VOLUME=/app/data
      # Whisper 配置
      - WHISPER_API_URL=http://aidg-whisper:80
      - WHISPER_MODE=go-whisper
      # 音频处理配置
      - ENABLE_AUDIO_CONVERSION=true
      - ENABLE_SPEAKER_DIARIZATION=true
      - ENABLE_DEGRADATION=true
      - ENABLE_OFFLINE=false
      # 基础配置
      - ENV=production
      - PORT=8000
      - MCP_HTTP_PORT=8081
      - LOG_LEVEL=info
      - LOG_FORMAT=json
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
```

**关键环境变量说明：**

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DEPENDENCY_MODE` | `remote` | 依赖服务模式（remote=使用deps-service容器）|
| `DEPS_SERVICE_URL` | `http://aidg-deps-service:8080` | deps-service 访问地址 |
| `WHISPER_API_URL` | `http://aidg-whisper:80` | Whisper 服务访问地址 |
| `ENABLE_OFFLINE` | `false` | 是否启用离线模式（false允许下载模型）|
| `HUGGINGFACE_TOKEN` | 必需 | HuggingFace访问令牌（用于PyAnnote）|
| `ENV` | `production` | 运行环境（production/development）|
| `LOG_LEVEL` | `info` | 日志级别（debug/info/warn/error）|
| `LOG_FORMAT` | `json` | 日志格式（json/console）|

---

## 🌟 开始使用

### 首次登录

1. 打开浏览器，访问 http://localhost:8000
2. 使用默认账号登录：
   - 用户名：`admin`
   - 密码：`admin123`
3. **立即修改密码**（重要！）

### 创建第一个项目

1. 点击左侧的 "+"（创建项目）
2. 输入项目名称，如 "我的第一个项目"
3. 点击创建

### 创建第一个任务

1. 在项目中点击 "新建任务"
2. 输入任务名称和描述
3. 开始工作！

### 连接 AI 助手（使用 MCP）

如果你使用 Claude Desktop 或其他支持 MCP 的工具：

1. 在 Claude Desktop 配置文件中添加：
```json
{
  "mcpServers": {
    "aidg": {
      "command": "node",
      "args": ["/path/to/mcp-client.js"],
      "env": {
        "MCP_SERVER_URL": "http://localhost:8081"
      }
    }
  }
}
```

2. 重启 Claude Desktop
3. 现在可以让 AI 帮你查项目资料了！

---

## ⚙️ 高级配置

### 修改端口

如果默认端口被占用，可以修改：

编辑 `docker-compose.yml` 或 `docker-compose.deps.yml`：
```yaml
ports:
  - "你的端口:8000"  # 比如改成 "9000:8000"
```

### 数据备份

定期备份你的数据目录：
```bash
# 创建备份
tar -czf aidg-backup-$(date +%Y%m%d).tar.gz data/

# 恢复备份
tar -xzf aidg-backup-20250114.tar.gz
```

### 查看日志

```bash
# 基础版：查看所有服务日志
docker-compose logs -f

# 完整版：查看所有服务日志
docker-compose -f docker-compose.deps.yml logs -f

# 查看特定服务日志（使用容器名称）
docker logs aidg-unified -f          # 主服务
docker logs aidg-whisper -f          # Whisper服务
docker logs aidg-deps-service -f     # Deps服务

# 只查看最近的日志
docker logs aidg-unified --tail 100
```

### 停止服务

```bash
# 基础版：停止所有服务
docker-compose down

# 完整版：停止所有服务
docker-compose -f docker-compose.deps.yml down

# 停止但保留数据卷
docker-compose -f docker-compose.deps.yml stop
```

### 更新镜像

```bash
# 基础版：拉取最新镜像
docker-compose pull

# 完整版：拉取最新镜像
docker-compose -f docker-compose.deps.yml pull

# 重新构建并启动
docker-compose -f docker-compose.deps.yml up -d --build
```

---

## 常见问题

### Q1: Docker 启动失败，提示 "Cannot connect to Docker daemon"

**答**：Docker Desktop 可能没有运行。
- **解决方法**：打开 Docker Desktop，确保它在运行（托盘图标应该是正常状态）

### Q2: 端口被占用，提示 "port is already allocated"

**答**：8000、8081 等端口被其他程序占用了。
- **解决方法**：
  - 找到占用端口的程序并关闭它
  - 或者修改配置文件，使用其他端口（参考[修改端口](#修改端口)）

### Q3: deps-service 构建失败

**答**：可能是网络问题或磁盘空间不足。
- **解决方法**：
  1. 检查网络连接
  2. 确保有至少 10GB 空闲磁盘空间
  3. 尝试使用国内镜像加速（见下方）

**配置 Docker 镜像加速（国内用户）：**
```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://registry.docker-cn.com"
  ]
}
```

### Q4: HuggingFace Token 无效

**答**：Token 可能输入错误或过期。
- **解决方法**：
  1. 重新访问 https://huggingface.co/settings/tokens
  2. 确认 Token 有效且有 "Read" 权限
  3. 检查环境变量是否正确设置

### Q5: 会议录音转写失败

**答**：可能是音频格式不支持或服务未启动。
- **解决方法**：
  1. 确认所有服务都在运行（`docker-compose -f docker-compose.deps.yml ps`）
  2. 检查服务状态：访问 http://localhost:8000/api/v1/services/status
  3. 支持的音频格式：WAV、MP3、M4A、WebM
  4. 检查服务日志：
     - Whisper: `docker logs aidg-whisper`
     - Deps-service: `docker logs aidg-deps-service`
     - 主服务: `docker logs aidg-unified`

### Q6: 内存不足，服务频繁重启

**答**：完整版需要较多内存。
- **解决方法**：
  1. 增加 Docker Desktop 的内存限制（设置 → Resources → Memory）
  2. 或者使用基础版，不需要会议功能

### Q7: 如何卸载？

**答**：
```bash
# 基础版：停止并删除所有容器
docker-compose down

# 完整版：停止并删除所有容器
docker-compose -f docker-compose.deps.yml down

# 删除镜像（可选）
docker rmi aidg-aidg:lite-test
docker rmi aidg-deps-service:latest
docker rmi ghcr.io/mutablelogic/go-whisper:latest

# 删除数据目录（谨慎！会丢失所有数据）
rm -rf data/

# 删除模型缓存（可选，会重新下载）
rm -rf models/
```

---

## 🔒 安全建议

### 生产环境部署

如果你要在生产环境使用，**务必修改这些配置**：

1. **修改密码**：
```yaml
environment:
  - ADMIN_DEFAULT_PASSWORD=你的强密码（至少12位）
  - MCP_PASSWORD=你的MCP强密码
```

2. **修改 JWT Secret**：
```yaml
environment:
  - JWT_SECRET=生成一个至少32位的随机字符串
  - USER_JWT_SECRET=生成另一个32位随机字符串
```

**生成随机字符串的方法：**
```bash
# Linux/Mac
openssl rand -base64 32

# 或者使用在线工具
# https://www.random.org/strings/
```

3. **限制访问**：
```yaml
environment:
  - CORS_ALLOWED_ORIGINS=https://你的域名.com
```

4. **使用 HTTPS**：
   - 在生产环境中使用 Nginx 或 Caddy 作为反向代理
   - 配置 SSL 证书（Let's Encrypt 免费）

---

## 📊 性能优化

### 基础版优化

- **内存**: 最少 2GB，推荐 4GB
- **CPU**: 2核以上
- **磁盘**: SSD 会显著提升性能

### 完整版优化

**资源配置（根据 docker-compose.deps.yml）：**

**Whisper 服务：**
- **最小配置**: 1核CPU，1GB内存
- **推荐配置**: 2核CPU，2GB内存
- **磁盘**: 约500MB（模型缓存）

**Deps-service（FFmpeg + PyAnnote）：**
- **最小配置**: 2核CPU，4GB内存
- **推荐配置**: 4核CPU，8GB内存
- **磁盘**: 约5GB（PyAnnote模型 + HuggingFace缓存）

**主服务（aidg-unified）：**
- **最小配置**: 1核CPU，1GB内存
- **推荐配置**: 2核CPU，2GB内存
- **磁盘**: 根据数据量（项目、用户、会议记录）

**总体要求：**
- **内存**: 最少 6GB，推荐 12GB+
- **CPU**: 4核以上（会议转写会用到）
- **磁盘**: 
  - 系统: 至少 10GB 空闲
  - 模型缓存: 5-6GB（Whisper + PyAnnote）
  - 数据存储: 根据使用量预留（建议至少5GB）

### 加速技巧

1. **使用本地模型**：
   - 首次运行会下载模型，之后会缓存
   - 模型存储在 `./models` 目录

2. **网络优化**：
   - 使用镜像加速（国内用户）
   - 预先下载大文件

3. **定期清理**：
```bash
# 清理 Docker 缓存
docker system prune -a

# 清理旧的日志
docker-compose logs --tail=0
```

---

## 🆘 获取帮助

### 文档资源

- 📖 **完整文档**: [docs/](../docs/)
- 🏗️ **架构设计**: [ARCHITECTURE_MIGRATION.md](ARCHITECTURE_MIGRATION.md)
- 🐳 **Docker 指南**: [DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md)

### 社区支持

- 💬 **GitHub Issues**: https://github.com/houzhh15/AIDG/issues
- 📧 **邮件支持**: houzhh15@mails.tsinghua.edu.cn

### 日志分析

遇到问题时，请提供：
1. 操作系统版本
2. Docker 版本（`docker --version`）
3. 完整的错误日志（`docker-compose logs`）
4. 你的配置文件（去掉密码信息）

---

## 🎉 恭喜！

如果你走到这里，说明你已经成功部署了 AIDG！

### 下一步？

1. 📖 阅读[用户手册](USER_GUIDE.md)，了解更多功能
2. 🔗 连接你喜欢的 AI 助手（Claude、ChatGPT）
3. 🚀 开始你的第一个项目
4. 💬 加入社区，分享你的使用体验

**祝你使用愉快！** 🌟

---

*最后更新：2025-10-17*  
*根据 docker-compose.deps.yml 校准完成*  
*有任何问题或建议？欢迎提 Issue 或 PR！*
