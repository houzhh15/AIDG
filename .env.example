# AIDG 环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ============================================================================
# 基础配置
# ============================================================================

# 运行环境（必须是 'development' 或 'production'，不能是 'dev' 或 'prod'）
ENV=development

# Web Server 配置
PORT=8000

# MCP Server 配置
MCP_HTTP_PORT=8081

# 日志配置
LOG_LEVEL=info           # debug, info, warn, error
LOG_FORMAT=console       # console (开发), json (生产)

# ============================================================================
# 安全配置（生产环境必须修改！）
# ============================================================================

# JWT 密钥 - 用于 MCP Server 认证（至少 32 字符）
JWT_SECRET=change-me-to-a-random-secret-at-least-32-characters-long

# 用户 JWT 密钥 - 用于用户认证（至少 32 字符）
USER_JWT_SECRET=change-me-to-another-random-secret-at-least-32-chars

# 管理员默认密码（至少 8 字符）
ADMIN_DEFAULT_PASSWORD=ChangeMe123!

# MCP Server 密码
MCP_PASSWORD=change-me-mcp-password

# ============================================================================
# 数据目录配置
# ============================================================================

PROJECTS_DIR=/app/data/projects
USERS_DIR=/app/data/users
MEETINGS_DIR=/app/data/meetings
AUDIT_LOGS_DIR=/app/data/audit_logs

# ============================================================================
# 网络配置
# ============================================================================

# MCP Server URL（容器内部使用 localhost）
MCP_SERVER_URL=http://localhost:8000

# CORS 允许的源（多个用逗号分隔）
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000

# ============================================================================
# 前端配置
# ============================================================================

FRONTEND_DIST_DIR=./frontend/dist

# ============================================================================
# 安全提示
# ============================================================================
# 
# ⚠️  生产环境必须修改以下值：
#   - ENV: 改为 'production'
#   - JWT_SECRET: 使用强随机字符串（≥32 字符）
#   - USER_JWT_SECRET: 使用不同的强随机字符串（≥32 字符）
#   - ADMIN_DEFAULT_PASSWORD: 使用强密码（≥8 字符）
#   - MCP_PASSWORD: 使用强密码
#
# 💡 生成随机密钥：
#   openssl rand -base64 32
#   或
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 🔒 不要将 .env 文件提交到 Git！
# ============================================================================

# ============================================================================
# Whisper Service Configuration
# ============================================================================
# Whisper服务模式选择
WHISPER_MODE=go-whisper         # go-whisper | faster-whisper | local-whisper

# 自动降级配置
ENABLE_DEGRADATION=true         # true | false - 启用故障自动降级到Mock模式

# 健康检查配置
HEALTH_CHECK_INTERVAL=5m        # 健康检查间隔 (格式: 5m, 10s, 1h)
HEALTH_CHECK_FAIL_THRESHOLD=3   # 连续失败次数阈值 (触发降级前的失败次数)
