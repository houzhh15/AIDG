# Example HTTPS configuration for docker-compose
# Copy this file to .env and customize for your deployment
#
# For HTTPS deployment:
#   cp .env.https.example .env
#   # Edit .env with your values
#   docker-compose -f docker-compose.ghcr.yml up -d

# ===================================
# HTTPS Configuration
# ===================================
# Server protocol (http or https)
SERVER_PROTOCOL=https

# Server port (443 for standard HTTPS, 8443 for non-privileged)
SERVER_PORT=443

# TLS certificate file path (inside container)
TLS_CERT_FILE=/app/certs/server.crt

# TLS private key file path (inside container)
TLS_KEY_FILE=/app/certs/server.key

# ===================================
# Docker Image
# ===================================
# Image tag (latest, v0.1.1, or any published version)
IMAGE_TAG=latest

# ===================================
# Application Settings
# ===================================
# Environment (development, staging, production)
ENV=production

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# ===================================
# Security Credentials
# ===================================
# IMPORTANT: Change these in production!

# JWT secret for user authentication (at least 32 characters)
USER_JWT_SECRET=change-me-to-a-secure-random-string-at-least-32-chars

# Admin default password
ADMIN_DEFAULT_PASSWORD=change-me-to-secure-password

# MCP server password
MCP_PASSWORD=change-me-to-secure-mcp-password

# ===================================
# MCP Server Configuration
# ===================================
# MCP Server URL (should match SERVER_PROTOCOL and SERVER_PORT)
# For HTTPS with port 443: https://localhost:443
# For HTTPS with custom port: https://localhost:8443
MCP_SERVER_URL=https://localhost:443

# ===================================
# CORS Configuration
# ===================================
# Allowed origins for CORS (comma-separated)
# For HTTPS, update to use https:// URLs
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
